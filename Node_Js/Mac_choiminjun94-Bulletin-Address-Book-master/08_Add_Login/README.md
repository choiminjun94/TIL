1. npm install --save ejs express mongoose body-parser method-override 설치
2. npm install --save bcryptjs
3. npm install --save express-session connect-flash
4. npm install --save passport passport-local

5. 스키마는 데이터베이스를 구성하는 데이터 개체(Entity), 속성(Attribute), 관계(Relationship) 및 데이터 조작 시 데이터 값들이 갖는 제약 조건 등에 관해 전반적으로 정의한다.

6.아아아. 